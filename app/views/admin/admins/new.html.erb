<div class="flex">
  <h1 class="text-2xl text-gray-600">
    <%= t('.heading') %> /
    <span class="font-semibold">
      <%= t('.heading_subtitle')%>
    </span>
  </h1>
</div>

<%= render('form', admin: @admin) %>
